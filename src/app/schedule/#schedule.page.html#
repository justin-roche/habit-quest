    <ion-header style="height:5%" no-padding>
        <ion-grid no-padding no-margin>

            <ion-row no-padding no-margin>
                <ion-col size="2" no-padding no-margin ion-justify-content-center style="justify-content:center;text-align:center">
                    <ion-button (click)="back()" no-padding no-margin style="justify-content:center;text-align:center;height:70%">
                        <ion-icon name="arrow-back"></ion-icon>
                    </ion-button>
                </ion-col>

                <ion-col size="8">
                    {{ viewTitle }}
                </ion-col>
                <ion-col size="2" no-padding no-margin ion-justify-content-center style="justify-content:center;text-align:center">
                    <ion-button (click)="back()" no-padding no-margin style="justify-content:center;text-align:center;height:70%">
                        <ion-icon name="arrow-forward"></ion-icon>
                    </ion-button>
                </ion-col>

            </ion-row>
        </ion-grid>
    </ion-header>


    <ion-content>
        <calendar (onTimeSelected)="onTimeSelected($event)" [currentDate]="calendar.currentDate" [noEventsLabel]="''" [formatDayHeader]="" [calendarMode]="'month'" [showEventDetail]="false" [formatMonthTitle]="'MMMM '">
        </calendar>
        <!-- <calendar [currentDate]="calendar.currentDate" [calendarMode]="'day'"> -->
        <!-- </calendar> -->
    </ion-content>
    <ion-footer>
        <ion-button float-left (click)="dismiss()">
            Cancel
        </ion-button>

        <ion-button float-right (click)="dismiss()">
            Done
        </ion-button>


    </ion-footer>